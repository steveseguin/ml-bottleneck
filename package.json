{
  "name": "ml-bottleneck",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:playwright": "playwright test tests/website-playwright.spec.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
