{
  "name": "ml-bottleneck",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh:localmaxxing": "node scripts/refresh-localmaxxing.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:playwright": "playwright test tests/website-playwright.spec.mjs",
    "audit:gold": "node scripts/audit-gold-cases.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
